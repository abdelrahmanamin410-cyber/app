name: academy_app
description: Professional Academy mobile app (Flutter) - frontend
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  provider: ^6.0.5
  flutter_secure_storage: ^8.0.0
  cached_network_image: ^3.2.3
  flutter_svg: ^1.1.6
  intl: ^0.17.0
  lottie: ^3.3.2   # 👈 Add this line for Lottie support
  google_fonts: ^6.3.2
  shared_preferences: ^2.1.1
  salomon_bottom_bar: ^3.3.2
  video_player: ^2.10.0
  firebase_core: ^3.8.0
  cloud_firestore: ^5.5.0
  firebase_auth: ^5.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/background.png
    - assets/images/logo.png
    - assets/animations/Untitledg.mp4.lottie (2).json   # 👈 Add your Lottie file here
